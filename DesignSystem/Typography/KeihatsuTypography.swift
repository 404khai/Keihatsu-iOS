import SwiftUI

struct KeihatsuTypography {
    let hero = Font.system(.largeTitle, design: .rounded).weight(.bold)
    let sectionTitle = Font.system(.title3, design: .rounded).weight(.semibold)
    let cardTitle = Font.system(.headline, design: .rounded).weight(.semibold)
    let body = Font.system(.body, design: .rounded)
    let caption = Font.system(.caption, design: .rounded)
}
